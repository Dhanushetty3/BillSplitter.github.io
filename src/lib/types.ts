import { z } from 'zod';

// == AI Schemas and Types ==

// This file is being cleaned up to reduce complexity and avoid build issues
// with 'use server' modules. Types are now imported directly into the
// files that need them (e.g., src/app/actions.ts and src/app/page.tsx).


// == App Specific Types ==

// The DemoBillData and ActionResult types are defined directly
// in `src/app/actions.ts` where they are used.
